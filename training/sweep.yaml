program: train.py
method: bayes
metric:
  name: valid_loss
  goal: minimize
parameters:
  model_name:
    values: ['resnet34d', 'convnext_small_in22k'] 
  learning_rate:
    min: 0.01
    max: 0.02
  resize_method:
    values: ['crop', 'squish']

